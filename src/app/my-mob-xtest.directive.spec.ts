import { MyMobXTestDirective } from './my-mob-xtest.directive';

describe('MyMobXTestDirective', () => {
  it('should create an instance', () => {
    const directive = new MyMobXTestDirective();
    expect(directive).toBeTruthy();
  });
});
